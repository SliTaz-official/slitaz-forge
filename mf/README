Slitaz Mirror Files
================================================================================


Mirror Files host static SLiTaz files such as CSS style and images. It can and 
should be used by all mirror.slitaz.org vhost so we just have to work on these
to update all sites. Mf can also be used by other vhosts and on mirror symlink
can be used to point to MF:

# ln -s /home/slitaz/repos/slitaz-forge/mf . 


Vhost  : http://mf.slitaz.org/
Mirror : http://mirror.slitaz.org/


================================================================================

